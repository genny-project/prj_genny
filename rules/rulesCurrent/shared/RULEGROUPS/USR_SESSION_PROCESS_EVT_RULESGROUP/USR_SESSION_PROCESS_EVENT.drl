package life.genny.rules;

rule "USR_SESSION_PROCESS_EVT"
    ruleflow-group 'Process Event'
    lock-on-active true
    when
      $process : WorkflowProcessInstance( ) 

     then
     log.info("Send Project");


end
